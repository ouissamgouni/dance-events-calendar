# ════════════════════════════════════════════════════════════════
# BASE CONFIG — committed, no secrets
# Universal defaults applied in all environments.
# Override in config/dev.env, config/staging.env, config/prod.env, etc.
# ════════════════════════════════════════════════════════════════

# ── Database defaults (local dev/test/scenario only) ─────────────
# Remote environments (staging-remote, prod) override DATABASE_URL in config/secrets.*.env
POSTGRES_USER=calendar_user
POSTGRES_PASSWORD=calendar_password

# ── App defaults ─────────────────────────────────────────────────
SYNC_INTERVAL_MINUTES=15
APP_VERSION=1.0.0
SMTP_PORT=587
