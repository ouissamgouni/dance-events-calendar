# ════════════════════════════════════════════════════════════════
# PRODUCTION SECRETS TEMPLATE — copy to config/secrets.prod.env
# Gitignored — never commit.
#
# Used by:
#   task db:migrate:prod     — run Alembic migrations against Neon
#   task db:check:prod       — test connectivity + show migration state
#   task fly:secrets         — push all secrets to Fly.io
#   task deploy:prod:remote  — full prod deploy (migrate + fly + cloudflare)
#
# Non-secret remote prod config (CORS_ORIGINS, VITE_*) lives in
# config/prod.remote.env (committed) and config/fly.prod.toml.
# ════════════════════════════════════════════════════════════════

# ── Admin user ───────────────────────────────────────────────────
ADMIN_EMAIL=

# ── Database (Neon — main branch) ────────────────────────────────
# Full connection string from Neon dashboard (Pooled connection recommended)
DATABASE_URL=postgresql://user:password@ep-xxx-yyy.eu-central-1.aws.neon.tech/neondb?sslmode=require

# ── Session signing ──────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=

# ── Umami Cloud API key (optional) ───────────────────────────────
# Required only by `task analytics:reset:umami ENV=prod` to wipe Umami
# Cloud data via API. Get one at: https://cloud.umami.is → Settings → API.
UMAMI_API_KEY=

# ── Web Push (VAPID keys for push notifications) ──────────────────
# Generate new keypair with: npm install -g web-push && web-push generate-vapid-keys
# Store public key in prod config, private key here (secret).
WEB_PUSH_ENABLED=true
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
