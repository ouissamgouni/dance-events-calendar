# Override BASE_URL via perf/k6/profiles/staging.override.env (gitignored) if
# you want to point at a different host (e.g. movida-staging.fly.dev origin).
BASE_URL=https://api-develop.joinmovida.com
ENV=staging
VUS=15
DURATION=5m
RAMP=30s
THINK_MIN=4
THINK_MAX=10
