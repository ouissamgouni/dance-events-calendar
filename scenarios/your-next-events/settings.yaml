# Enables the explorer's "Your next events" rail. The rail lists the
# viewer's own saved + attending events, sorted by start date ascending.
# `for_you_rail_enabled` is intentionally OFF here so this scenario tests
# the Your-next-events rail in isolation.
settings:
  your_next_events_rail_enabled: true
  for_you_rail_enabled: false
