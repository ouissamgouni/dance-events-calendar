# Real email one-time-code login scenario.
#
# DEV_AUTH=false means the backend does NOT surface the code in the response —
# it is only delivered by email (SMTP). You must configure SMTP_* in secrets.env
# and use a real inbox to complete the flow.
CALENDAR_SERVICE=mock
DEV_AUTH=false
