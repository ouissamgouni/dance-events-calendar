# Auto-detection is intentionally OFF so the scenario exercises the manual
# "Scan now" action in the Series panel. Flip to true to instead observe a
# series being auto-grouped as part of the sync in step 1.
settings:
  series_auto_detect_enabled: false
