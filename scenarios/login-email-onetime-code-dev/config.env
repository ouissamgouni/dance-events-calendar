# Dev-mode email one-time-code login scenario.
#
# DEV_AUTH=true means the backend surfaces the generated 6-digit code back to
# the /login page (amber "dev code" box) and logs it, so you can complete the
# flow without a real mail server. No SMTP configuration is required.
CALENDAR_SERVICE=mock
DEV_AUTH=true
