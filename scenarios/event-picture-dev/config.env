CALENDAR_SERVICE=mock
DEV_AUTH=true
ADMIN_EMAIL=admin@example.com
# Object storage runs against the per-scenario MinIO emulator (default).
# Flip to r2 to point the same scenario at a real Cloudflare bucket.
OBJECT_STORAGE_PROVIDER=minio
