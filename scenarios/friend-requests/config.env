CALENDAR_SERVICE=mock
DEV_AUTH=true
# Phase E (E8) — explicitly enable the friend-requests flow. The flag
# defaults to "true" in dev, but pinning it here makes the scenario
# self-documenting and protects against env drift.
FEATURE_FRIEND_REQUESTS=true
