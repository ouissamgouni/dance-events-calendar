# Enable INSTANT email mode for friends_going, on top of the default
# digest mode, so this scenario also exercises the immediate (no-schedule)
# email path. With instant ON, a plain dispatch tick emails the row right
# away (stamps instant_emailed_at); the digest step still delivers a
# second email on the forced tick.
settings:
  friends_going_email_instant: true
