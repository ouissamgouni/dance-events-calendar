CALENDAR_SERVICE=mock
AUTO_SYNC_ENABLED=true
DEV_AUTH=true
ADMIN_EMAIL=admin@example.com

# This scenario validates the Umami analytics pipeline, so opt in to
# spinning up the Umami containers (disabled by default for all scenarios).
UMAMI_ENABLED=true
