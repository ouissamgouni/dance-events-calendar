# Mock users pre-seeded for this scenario.
# At /login, dev mode renders one "Sign in as <Name>" button per entry.
# Admin status is derived by matching the email to ADMIN_EMAIL.
users:
  - email: dev-user@example.com
    name: Dev User
  - email: rater@example.com
    name: Sam Rater
  - email: admin@example.com # matches ADMIN_EMAIL → admin
    name: Dev Admin
  # Extra reviewers used by pre-seeded ratings on evt-rating-001 (pagination
  # demo) and evt-rating-002 (mixed-status demo). They have signed in once so
  # the dev-mode login page lets you re-impersonate them if needed.
  - email: alice@example.com
    name: Alice Reviewer
  - email: bruno@example.com
    name: Bruno Reviewer
  - email: chloe@example.com
    name: Chloé Reviewer
  - email: dimitri@example.com
    name: Dimitri Reviewer
  - email: elena@example.com
    name: Elena Reviewer
